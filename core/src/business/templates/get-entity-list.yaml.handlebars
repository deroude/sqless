operations:
  - id: GetList{{ name }}
    template: >-
      SELECT {{#each properties}}"{{ nameSnake }}"{{~#unless @last}},{{/unless}}{{/each}} 
      FROM "{{ nameSnake }}" ;    
    singleRow: false
    assign: 
      var: List{{ name }}
return: List{{ name }}